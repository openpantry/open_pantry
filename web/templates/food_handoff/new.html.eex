<h2>New food handoff</h2>

<%= render "form.html", changeset: @changeset,
                        action: food_handoff_path(@conn, :create) %>

<%= link "Back", to: food_handoff_path(@conn, :index) %>
